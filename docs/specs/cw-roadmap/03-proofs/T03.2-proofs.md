# T03.2 Proof Summary: /cw-roadmap lint subcommand

Task: T03.2 — /cw-roadmap lint subcommand (native task ID #14)
Scope: skills/cw-roadmap/SKILL.md (modify only)
Worker: worker-9

## Changes Made

Two edits to SKILL.md:

1. **Added `### Subcommand Dispatch` section** at the top of the `## Process` section (before Step 1). Dispatches to L1–L5 when first arg is `lint`, otherwise proceeds with normal Steps 1–6.

2. **Replaced placeholder body of `### Step 7: Lint Subcommand`** with full L1–L5 lint flow:
   - L1: Parse args, locate file, validate existence (Read-only contract)
   - L2: Load assertions module via importlib from skill directory
   - L3: Run each assertion, collect Check/Status/Message rows
   - L4: Render 80-column table + summary line with FAILURE signal
   - L5: Return without invoking rest of Process

## Contract Compliance

| Requirement | Location in SKILL.md | Status |
|---|---|---|
| R3.3: dispatch lint subcommand | Subcommand Dispatch section (line 40) | PASS |
| R3.3: reads file at <path> | L1 step 2 (Read tool only) | PASS |
| R3.3: loads assertions.py via importlib | L2 (canonical importlib pattern) | PASS |
| R3.3: runs every function in ASSERTIONS | L3 step 3 (iterate all, no short-circuit) | PASS |
| R3.3: 3-column table Check/Status/Message | L4 (80-col, fixed widths 40/6/28) | PASS |
| R3.3: summary K/M assertions passed | L4 (explicit pass/fail variants) | PASS |
| R3.3: Check truncated at 40 chars | L4 (name[:37] + "...") | PASS |
| R3.4: never modifies input file | L1 + L5 read-only contract | PASS |
| R3.4: FAILURE in summary when K<M | L4 ("K/M assertions passed — FAILURE") | PASS |
| R3.4: subcommand at very top of Process | Dispatch section before Step 1 | PASS |
| R3.4: returns early after lint | L5 explicit stop + no further steps | PASS |
| assertions.py path via skill directory | L2 (Path(<skill-dir>) / "assertions.py") | PASS |

## Proof Artifacts

| File | Type | Status |
|---|---|---|
| T03.2-01-file.txt | file (SKILL.md section grep verification) | PASS |
| T03.2-02-cli.txt | cli (80-col table rendering + FAILURE signal) | PASS |
