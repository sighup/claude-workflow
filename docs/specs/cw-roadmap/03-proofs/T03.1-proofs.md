# T03.1 Proof Summary

Task: assertions.py — 15+ binary checks + docstring-as-message

## Artifacts

| File | Type | Status |
|------|------|--------|
| T03.1-01-test.txt | test | PASS |
| T03.1-02-file.txt | file | PASS |

## Summary

### T03.1-01-test.txt — pytest run

Ran `python3 -m pytest tests/test_assertions.py -v` from `skills/cw-roadmap/`.

Result: 81 passed in 0.03s.

Each of the 17 assertions has at least 1 passing and 1 failing test case (well above the 30+ minimum).
The contract test confirms ASSERTIONS is exported, all entries are callable with a single `str` parameter,
all have docstrings, and the canonical GOOD_ROADMAP (150 lines) passes all assertions.

### T03.1-02-file.txt — file existence and assertion inventory

Confirmed:
- `skills/cw-roadmap/assertions.py` exists and exports `ASSERTIONS` list with 17 entries
- `skills/cw-roadmap/tests/test_assertions.py` exists with 81 tests
- All functions satisfy the contract: `(response: str) -> bool`, one-line docstring, no eval/shell/file-write

## Coverage Map

| R3.1 Check | Assertion Function |
|---|---|
| Section order | assert_section_order |
| Line count 150-250 | assert_line_count |
| Slice cardinality 5-8 | assert_slice_cardinality |
| Per-slice required fields | assert_slice_required_fields |
| Traces line per slice | assert_slice_traces_line |
| Exit signal verb | assert_exit_signal_verb |
| Delivers bullet count 3-6 | assert_delivers_bullet_count |
| DAG acyclic | assert_dag_acyclic |
| Depends on resolve | assert_depends_on_resolve |
| Traces PRD section format | assert_traces_prd_section_format |
| Maturity checkpoints rows >= 3 | assert_maturity_checkpoints_rows |
| Scope exclusion count >= 3 | assert_scope_exclusion_count |
| Scope exclusion rationale | assert_scope_exclusion_rationale |
| No body backtick fences | assert_no_body_backtick_fences |
| No headings > H3 in slices | assert_no_deep_headings_in_slices |
| Sequencing principles 4-6 | assert_sequencing_principles_count |
| Meta-Prompt block | assert_meta_prompt_block |
