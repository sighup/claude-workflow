Type: file
Command: grep -n "WebFetch\|Local file\|Image file\|Local directory\|Web URL\|classify" skills/cw-research/SKILL.md
Expected: SKILL.md contains external context source handling instructions covering web, filesystem, and image sources
Timestamp: 2026-02-26T12:00:00Z

Output:
275:For each source the user provides, classify it and process accordingly:
279:| Web URL | Starts with `http://` or `https://` | Fetch with `WebFetch` |
280:| GitHub URL | Contains `github.com` | Fetch with `WebFetch` or use `Bash` with `gh` CLI for issues/PRs |
281:| Local file | Starts with `/`, `./`, `../`, or `~` | Read with `Read` tool |
282:| Local directory | Path ending with `/` or detected as directory | Explore with `Glob` and `Read` |
283:| Image file | Extensions: `.png`, `.jpg`, `.jpeg`, `.gif`, `.svg`, `.webp` | Read with `Read` tool (multimodal), describe content in report |
284:| Search query | Does not match URL or path patterns | Search with `WebSearch` |

Verification:
- Step 5b "Classify and process each source" at line 273 contains a source type classification table
- Web sources: WebFetch for general URLs, WebFetch or gh CLI for GitHub URLs
- Filesystem sources: Read tool for local files, Glob+Read for directories
- Image sources: Read tool (multimodal) with description in report
- Search queries: WebSearch as fallback
- Source examples in Step 5a include: GitHub issues/PRs, Jira tickets, Confluence pages,
  Google Docs, filesystem paths, screenshots/diagrams, web URLs, documentation links,
  knowledgebase articles, and ADRs
- Multiple sources accepted in single interaction (line 271)
- Source type table at lines 277-284 covers all required categories

Status: PASS
